{
  "name": "telegram-chat-bot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "persona": "node scripts/select-persona.mjs",
    "prepare": "node scripts/select-persona.mjs",
    "predev": "node scripts/select-persona.mjs",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "pretest": "node scripts/select-persona.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "pretypecheck": "node scripts/select-persona.mjs",
    "typecheck": "tsc --noEmit",
    "precheck": "node scripts/select-persona.mjs",
    "check": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^5.5.2",
    "vitest": "~3.2.0",
    "wrangler": "^4.102.0"
  }
}
